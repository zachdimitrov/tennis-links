@using TennisLinks.Web.Models.Messages

@model MessageViewModel

<div class="message-details-wrapper col-sm-6">
    <div class="message-details">
        <img src="~/Content/Images/tennis-default.png" alt="Player" />
        <h2>from @Model.Author</h2>

        <table class="table">
            <tr>
                <th scope="row">Date received:</th>
                <td>@Model.CreatedOn</td>
            </tr>
            <tr>
                @if (@Model.Title.Length > 0)
                {
                    <th scope="row">@Model.Title</th>
                }
                else
                {
                    <th scope="row">No title!</th>
                }
                <td>@Model.Content</td>
            </tr>
        </table>
        <a href="/messages/write?recipient=@Model.Author" id="msg-@Model.Author" class="btn btn-sm btn-success">REPLY</a>
    </div>
</div>